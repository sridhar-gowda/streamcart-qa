# Selenium Grid / docker compose (`selenium/standalone-chrome`).
# Override the endpoint per environment with STREAMCART_WEB__REMOTE_URL.
web:
  browser: chrome
  headless: true
  remote_url: http://localhost:4444
